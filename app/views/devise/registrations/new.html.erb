<%# <%= render %>

<body>
<%= form_with model:@user, url: user_session_path, class: 'registration-main', local: true do |f| %>
<div class="login-wrap">
  <div class="login-html">
    <label for="tab-2" class="tab">Sign Up</label>
    <div class="login-form">
      <div class="sign-up-htm">
        <div class="group">
          <label class="label">Username</label>
          <%= f.text_area :name, class: "input-default", id:"name", placeholder: "" %>
        </div>
        <div class="group">
          <label class="label">Email</label>
          <%= f.email_field :email, class: "input-default", id:"email", placeholder: "PC/携帯どちらでも可", autofocus: true %>
        </div>
        <div class="group">
          <label class="label">Password</label>
          <%= f.password_field :password, class:"input-default", id:"password", placeholder: "英数字含めて設定してください" %>
        </div>
        <div class="group">
          <label class="label">Repeat Password</label>
          <%= f.password_field :password_confirmation, class: "input-default", id: "password_confirmation", placeholder: "同じパスワードを入力してください" %>
        </div>
        <div class="group">
          <%= f.submit "Sign UP", class:"register-btn" %>
        </div>
        <div class="hr"></div>
      </div>
    </div>
  </div>
</div>
<% end %>
</body>

<%# <%= render %>